import { ADDRESS_STATES } from "./address.constants";
import { MARITAL_STATUS } from "./maritalStatus.constants";
import { LICENSE_CATEGORIES } from "./licenseCategory.constants";
import { NOTIFICATION_TIME_IN_SECONDS, QUERY_STALE_TIME_IN_SECONDS } from "./global.constants";

export {
  ADDRESS_STATES,
  MARITAL_STATUS,
  LICENSE_CATEGORIES,
  QUERY_STALE_TIME_IN_SECONDS,
  NOTIFICATION_TIME_IN_SECONDS,
};
